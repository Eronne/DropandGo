{# Footer #}
<div class="ui inverted vertical footer segment">
    <div class="ui center aligned container">
        <div class="ui stackable inverted divided grid">
            <div class="three wide column">
                <h4 class="ui inverted header">{{ 'footer.links.title' | trans() }}</h4>
                <div class="ui inverted link list">
                    <a href="{{ path('homepage') }}" class="item">{{ 'footer.links.first' | trans }}</a>
                    <a href="{{ path('help') }}" class="item">{{ 'footer.links.second' | trans }}</a>
                    <a href="{{ path('fos_user_registration_register') }}" class="item">{{ 'footer.links.third' | trans }}</a>
                    <a href="{{ path('fos_user_security_login') }}" class="item">{{ 'footer.links.fourth' | trans }}</a>
                </div>
            </div>
            <div class="three wide column">
                <h4 class="ui inverted header">{{ 'footer.grp2.title' | trans() }}</h4>
                <div class="ui inverted link list">
                    <a href="{{ path(app.request.get('_route'), app.request.get('_route_params') | merge({'_locale' : 'fr' })) }}" class="item">Français</a>
                    <a href="{{ path(app.request.get('_route'), app.request.get('_route_params') | merge({'_locale' : 'en' })) }}" class="item">English</a>
                </div>
            </div>
            <div class="seven wide column">
                <h3 class="ui inverted header">Drop&Go</h3>
                <p>{{ 'footer.description' | trans() }}</p>
            </div>
        </div>
        <div class="ui inverted section divider"></div>
        <h1>Drop&Go</h1>
        <div class="ui horizontal inverted small divided link list">
            <a class="item" href="#">Site Map</a>
            <a class="item" href="#">Contact Us</a>
            <a class="item" href="#">Terms and Conditions</a>
            <a class="item" href="#">Privacy Policy</a>
        </div>
    </div>